# 组件按 Tab 分类抽离 - 重构总结

## ✅ 完成的工作

### 1. 组件分类与迁移

#### 📦 共享组件（保留在 `src/components/task/`）
被多个页面使用的通用组件：
- ✅ `TaskCard` - 任务卡片（home、todo 使用）
- ✅ `TaskCategoryTabs` - 分类标签（home、todo 使用）
- ✅ `TaskFilterSheet` - 筛选面板（home、todo 使用）
- ✅ `TaskTag` - 任务标签
- ✅ `types.ts` - 类型定义

#### 📂 页面专属组件（按 Tab 分类）

**我的待办页** (`view/todo/components/`)
- ✅ `TaskListGrouped` - 分组任务列表（按日期分组展示）

**我的消息页** (`view/message/components/`)
- ✅ `TaskOverdueList` - 逾期任务列表

**个人中心页** (`view/profile/components/`)
- ✅ `ProfileOverview` - 个人概览面板（已存在）

### 2. 导入路径更新

#### ✅ 首页 (`view/home/index.tsx`)
```typescript
import {
  TaskCard,
  TaskCategoryTabs,
  TaskFilterSheet,
  type TaskCategoryTab,
  type TaskFilterOption,
  TASK_CATEGORY_LABELS,
} from '@/components/task';
```

#### ✅ 我的待办 (`view/todo/index.tsx`)
```typescript
import { TaskFilterSheet, type TaskCategoryTab, type TaskFilterOption } from '@/components/task';
import { TaskListGrouped } from './components';
```
- 添加了内部筛选状态管理
- 修复了 `onFilterClick` 属性缺失的 linter 错误

#### ✅ 我的消息 (`view/message/index.tsx`)
```typescript
import { TaskOverdueList } from './components';
```

### 3. 导出文件更新

#### ✅ `components/task/index.ts`
```typescript
export { default as TaskCard } from './task-card';
export { default as TaskCategoryTabs } from './task-category-tabs';
export { default as TaskFilterSheet } from './task-filter-sheet';
export { default as TaskTag } from './task-tag';
export * from './types';
```

#### ✅ 各页面 `components/index.ts`
- `view/home/components/index.ts` - 空（无专属组件）
- `view/todo/components/index.ts` - 导出 `TaskListGrouped`
- `view/message/components/index.ts` - 导出 `TaskOverdueList`
- `view/profile/components/index.ts` - 导出 `ProfileOverview`

### 4. 文档更新

#### ✅ 更新的文档
- `src/view/README.md` - 页面组件结构说明
- `src/components/task/README.md` - Task 共享组件说明

## 🎯 重构原则

### 组件分类依据
1. **共享组件** - 被 2 个或以上页面使用
2. **专属组件** - 仅被单个页面使用

### 导入规范
- 共享组件：`@/components/task`
- 专属组件：`./components`

## 🚀 优势

1. **清晰的组件边界** - 共享组件和专属组件分离明确
2. **更好的 Tree-shaking** - 按需加载，未使用的组件不会打包
3. **易于维护** - 组件职责明确，便于定位和修改
4. **可扩展性强** - 新增组件遵循统一规范
5. **代码组织合理** - 符合 Tab 页面的业务逻辑划分

## 🔍 发现的问题

### 待清理的空文件夹
以下文件夹为空，建议手动删除：
- `src/components/task/task-list-grouped/`（空文件夹）
- `src/components/task/task-overdue-list/`（空文件夹）
- `src/components/task/task-board/`（空文件夹）
- `src/view/home/components/task-filter-sheet/`（空文件夹）

### 未使用的组件
- `src/components/task/task-profile-panel/` - 未被任何页面引用

## 🐛 修复的问题

### ✅ Linter 错误修复

**问题 1**: `src/router/index.tsx:26:21` - 类型 "{}" 中缺少属性 "onFilterClick"

**解决方案**: 
- 移除 `TodoPageProps` 接口
- 在 `TodoPage` 组件内部管理筛选状态
- 添加 `TaskFilterSheet` 组件到待办页

**问题 2**: `src/context/TaskContext.tsx:2:61` - 找不到模块 "../view/todo/components/types"

**解决方案**:
- 更新类型导入路径为 `@/components/task`
- 确保所有类型定义从统一的共享类型文件导入

**问题 3**: 运行时错误 - `useTaskContext must be used within TaskProvider`

**解决方案**:
- 在 `App.tsx` 中添加 `TaskProvider` 包裹
- 确保所有使用 `useTaskContext` 的组件都在 Provider 内部

## 📊 重构统计

- ✅ 移动组件数量：2 个（TaskListGrouped, TaskOverdueList）
- ✅ 共享组件数量：5 个（TaskCard, TaskCategoryTabs, TaskFilterSheet, TaskTag, types.ts）
- ✅ 更新文件数量：10+ 个
- ✅ Linter 错误：0 个
- ✅ 文档更新：2 个

## 📝 后续建议

1. **清理空文件夹** - 手动删除已迁移后的空文件夹
2. **清理未使用组件** - 确认并删除 `task-profile-panel`（如不需要）
3. **测试功能** - 运行项目确保所有页面功能正常
4. **考虑 Code Splitting** - 如果某些组件体积较大，可以考虑使用 React.lazy 进行懒加载

## 🎉 完成状态

所有任务已完成！项目组件已成功按 Tab 页面进行分类抽离。

